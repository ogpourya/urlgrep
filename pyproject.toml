[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "urlgrep"
version = "1.0.0"
description = ""
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.13.3",
    "uvloop>=0.22.1",
]

[project.scripts]
urlgrep = "urlgrep:run"
